# Vanilla-vite
Simple Vite package for front end apps.

## Stack
Pug, Sass, normalize.css, JS.

## Getting started
To start dev mode:
```shell
  npm install
  npm run dev
```
or to build app:
```shell
  npm run build
```
